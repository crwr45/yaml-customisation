Attempting to build the same using Kustomize. Failed when attempting to create duplicates of the `node`. It is possible but not at all simply.

```
kustomize build kustomize/overlays/lab.diktio.net
```
